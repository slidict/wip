# Minimal development image for the `web` service in this example's compose.yml.
# Replace with your app's real Dockerfile — this only exists so `wip build` has something to run.
FROM node:20-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm install

COPY . .

EXPOSE 3000
CMD ["npm", "run", "dev"]
