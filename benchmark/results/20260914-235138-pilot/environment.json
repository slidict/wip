{
  "capturedAt": "2026-09-14T23:51:38+09:00",
  "purpose": "confirmation pilot run after fixing three harness bugs found in the 20260914-225724 pilot (ProcessStartInfo.ArgumentList no-op on Windows PowerShell 5.1, localhost hanging vs 127.0.0.1, Start-Process -ArgumentList not quoting a value containing a space). Same environment as the prior pilot; see that run's environment.json for full host/WSL/Docker Desktop/wip details, unchanged.",
  "previousRun": "benchmark/results/20260914-225724-pilot",
  "wipRepoState": {
    "branch": "codex/add-wip-benchmark-skill",
    "commitAtRunStart": "a22e3ff"
  },
  "businessContainers": {
    "note": "www-* containers (Rails/Vue2/Vue3/Postgres/Redis/https-portal) were left stopped after the prior pilot's incident and confirmed still stopped immediately before this run (docker not running, wsl --list --verbose shows all distros Stopped).",
    "graceQuitFix": "Stop-DockerDesktopInfra now attempts a graceful CloseMainWindow() quit (up to 45s) before Stop-Process -Force, to reduce the chance of repeating that incident."
  }
}
