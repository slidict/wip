# wip ↔ Microsoft `wslc compose`: recorded differences

<!--
  Generated by Wip.Tests.ComposeUpstreamCompatTests. Do not edit by hand:

    WIP_COMPOSE_COMPAT_UPDATE=1 dotnet test --project tests/Wip.Tests/Wip.Tests.csproj

  Edit tests/compose-compat/matrix.json (statuses and notes) or the fixtures, then
  re-record. tests/compose-compat/README.md explains the workflow.
-->

Upstream is pinned at the commit named in
[`upstream/PINNED.md`](upstream/PINNED.md). `wip` column and `upstream` column are
the two readings of the same fixture, projected onto the one feature named on the
row.

## Summary

| status | entries |
|---|---|
| `compatible` | 13 |
| `known-difference` | 11 |
| `upstream-unsupported` | 1 |
| `wip-only` | 6 |

## Compatible — both implementations agree

### `image` — [01-basic-service](fixtures/01-basic-service/)

- **wip**: `{"app":"ruby:4"}`
- **upstream**: `{"app":"ruby:4"}`
- **status**: `compatible`
- **note**: Both take image: verbatim as the container image reference.

### `command` — [01-basic-service](fixtures/01-basic-service/)

- **wip**: `{"app":["ruby","-v"]}`
- **upstream**: `{"app":["ruby","-v"]}`
- **status**: `compatible`
- **note**: Exec form (a YAML list) reaches the container as the same argv on both sides. wip round-trips it through Shellwords join/split; upstream passes the list through.

### `environment` — [01-basic-service](fixtures/01-basic-service/)

- **wip**: `{"app":["RAILS_ENV=development"]}`
- **upstream**: `{"app":["RAILS_ENV=development"]}`
- **status**: `compatible`
- **note**: A mapping becomes the same ordered KEY=VALUE list on both sides.

### `working_dir` — [01-basic-service](fixtures/01-basic-service/)

- **wip**: `{"app":"/app"}`
- **upstream**: `{"app":"/app"}`
- **status**: `compatible`
- **note**: An absolute path is taken as written on both sides (wip as `-w`, upstream as SetWorkingDirectory).

### `ports` — [01-basic-service](fixtures/01-basic-service/)

- **wip**: `{"app":[{"host_ip":null,"host_port":"3000","container_port":"3000","protocol":"tcp"}]}`
- **upstream**: `{"app":[{"host_ip":null,"host_port":"3000","container_port":"3000","protocol":"tcp"}]}`
- **status**: `compatible`
- **note**: Plain host:container resolves to the same tcp mapping on both sides.

### `container_name` — [01-basic-service](fixtures/01-basic-service/)

- **wip**: `{"app":"app"}`
- **upstream**: `{"app":"app"}`
- **status**: `compatible`
- **note**: With no container_name:, both name the container after the service.

### `service_name` — [01-basic-service](fixtures/01-basic-service/)

- **wip**: `["app"]`
- **upstream**: `["app"]`
- **status**: `compatible`
- **note**: Both key services off the services: mapping in declaration order.

### `service_alias` — [01-basic-service](fixtures/01-basic-service/)

- **wip**: `{"app":["app"]}`
- **upstream**: `{"app":["app"]}`
- **status**: `compatible`
- **note**: Both resolve the service name on the project network. wip relies on wslc registering the container's --name; upstream calls AddPrimaryNetworkAlias explicitly.

### `environment` — [03-environment-list-form](fixtures/03-environment-list-form/)

- **wip**: `{"app":["RAILS_ENV=development","DATABASE_URL=postgres://db/app"]}`
- **upstream**: `{"app":["RAILS_ENV=development","DATABASE_URL=postgres://db/app"]}`
- **status**: `compatible`
- **note**: Both accept the list form and produce the same entries in the same order.

### `service_name` — [09-multi-service-network](fixtures/09-multi-service-network/)

- **wip**: `["web","db"]`
- **upstream**: `["web","db"]`
- **status**: `compatible`
- **note**: Both enumerate the services in declaration order.

### `service_alias` — [09-multi-service-network](fixtures/09-multi-service-network/)

- **wip**: `{"web":["web"],"db":["db"]}`
- **upstream**: `{"web":["web"],"db":["db"]}`
- **status**: `compatible`
- **note**: Each service resolves under its own name, so `postgres://db/...` works the same on both sides.

### `image` — [09-multi-service-network](fixtures/09-multi-service-network/)

- **wip**: `{"web":"ruby:4","db":"postgres:16"}`
- **upstream**: `{"web":"ruby:4","db":"postgres:16"}`
- **status**: `compatible`
- **note**: Both resolve each service's image independently.

### `ports` — [09-multi-service-network](fixtures/09-multi-service-network/)

- **wip**: `{"web":[{"host_ip":null,"host_port":"3000","container_port":"3000","protocol":"tcp"}],"db":[]}`
- **upstream**: `{"web":[{"host_ip":null,"host_port":"3000","container_port":"3000","protocol":"tcp"}],"db":[]}`
- **status**: `compatible`
- **note**: Only the service that declares ports: publishes any.

## Known differences — both have an opinion, and they differ

### `volumes` — [01-basic-service](fixtures/01-basic-service/)

- **wip**: `{"app":[{"type":"bind","source":".","source_base":"process-cwd","target":"/app","read_only":false}]}`
- **upstream**: `{"app":[{"type":"bind","source":"\u003CFIXTURE\u003E","source_base":"compose-file-dir","target":"/app","read_only":false}]}`
- **status**: `known-difference`
- **note**: A relative bind source resolves against a different directory: upstream against compose.yml's own directory (Compose Specification behaviour), wip against wip's process directory, because wip forwards the spec string to `wslc run -v` and GetFullPathNameW resolves it there. Upstream is right; this is a wip bug to fix before migration, not something to record and keep.

### `network` — [01-basic-service](fixtures/01-basic-service/)

- **wip**: `{"project":"basic","network":"basic"}`
- **upstream**: `{"project":"compose","network":"compose_default"}`
- **status**: `known-difference`
- **note**: Both derive a per-project network, differently and both incorrectly. Compose names it <project>_default with <project> from the project directory: wip uses the bare project name with no _default suffix, upstream appends _default but derives <project> from the compose file's stem, which its own source marks 'TODO: Implement this properly'. Adopt neither spelling; converge on the Specification's.

### `port_binding_default` — [01-basic-service](fixtures/01-basic-service/)

- **wip**: `"wslc-session-default"`
- **upstream**: `"127.0.0.1"`
- **status**: `known-difference`
- **note**: Where an unqualified published port lands. Upstream hardcodes 127.0.0.1 in CreateComposeContainers; wip forwards to `wslc run -p`, which uses the session's configured binding address. Not a compose.yml-level difference — it decides whether a published port is reachable off-box, so it is tracked separately from `ports`.

### `command` — [02-command-shell-form](fixtures/02-command-shell-form/)

- **wip**: `{"app":["bundle","exec","rails","server","-b","0.0.0.0"]}`
- **upstream**: `{"app":["bundle","exec","rails","server","-b","\u00220.0.0.0\u0022"]}`
- **status**: `known-difference`
- **note**: wip applies POSIX word splitting, so `-b "0.0.0.0"` reaches the container as 0.0.0.0. Upstream splits the scalar on single spaces and keeps the quotes as literal characters, giving "0.0.0.0"; its source marks this 'TODO: Implement proper parsing'. wip matches the Compose Specification here — do not follow upstream while the TODO stands.

### `environment` — [04-environment-passthrough](fixtures/04-environment-passthrough/)

- **wip**: `{"error":"\u003CFIXTURE\u003E/compose.yml: services.app.environment.FROM_HOST must have a value (host environment pass-through is not supported)"}`
- **upstream**: `{"app":["FROM_HOST="]}`
- **status**: `known-difference`
- **note**: Neither implements the Specification. Upstream turns `FROM_HOST:` into FROM_HOST= — an empty value, silently. wip refuses the file and says host pass-through is unsupported. Failing loudly is the better of the two, so keep wip's behaviour until one of them implements pass-through properly.

### `container_name` — [05-container-name](fixtures/05-container-name/)

- **wip**: `{"error":"\u003CFIXTURE\u003E/compose.yml: services.app has unsupported key(s): container_name"}`
- **upstream**: `{"app":"custom-app"}`
- **status**: `known-difference`
- **note**: Upstream honours container_name: (Specification behaviour); wip rejects the key outright. This is a real wip gap and the direction of travel is towards upstream — supporting the key is a prerequisite for migrating a project that uses it.

### `service_alias` — [05-container-name](fixtures/05-container-name/)

- **wip**: `{"error":"\u003CFIXTURE\u003E/compose.yml: services.app has unsupported key(s): container_name"}`
- **upstream**: `{"app":["custom-app"]}`
- **status**: `known-difference`
- **note**: Upstream aliases the *container* name, so container_name: silently renames the network alias too. The Specification aliases the service name and leaves it alone when container_name: is set, which is what makes `db:5432` keep working. Do not adopt upstream's coupling; fix wip's container_name gap without it.

### `container_name` — [06-upstream-name-key](fixtures/06-upstream-name-key/)

- **wip**: `{"error":"\u003CFIXTURE\u003E/compose.yml: services.app has unsupported key(s): name"}`
- **upstream**: `{"app":"wslc-named-app"}`
- **status**: `known-difference`
- **note**: Upstream accepts `name:` as a synonym for container_name:, and prefers it when both are set. The Compose Specification has no service-level `name:` (it is the top-level project name), so wip's rejection is correct. Track it so a compose.yml written against the POC is recognised, but do not implement it.

### `volumes` — [07-volume-forms](fixtures/07-volume-forms/)

- **wip**: `{"app":[{"type":"volume","source":"pgdata","source_base":null,"target":"/var/lib/postgresql/data","read_only":false},{"type":"bind","source":"./conf","source_base":"process-cwd","target":"/etc/app","read_only":true},{"type":"bind","source":"/srv/shared","source_base":"process-cwd-drive","target":"/mnt/shared","read_only":false}]}`
- **upstream**: `{"app":[{"type":"volume","source":"pgdata","source_base":null,"target":"/var/lib/postgresql/data","read_only":false},{"type":"bind","source":"\u003CFIXTURE\u003E/conf","source_base":"compose-file-dir","target":"/etc/app","read_only":true},{"type":"bind","source":"\u003CDRIVE\u003E/srv/shared","source_base":"compose-file-drive","target":"/mnt/shared","read_only":false}]}`
- **status**: `known-difference`
- **note**: Named volume and read-only flag agree. Bind sources do not: upstream anchors a relative source at compose.yml's directory and a driveless /srv path at the compose file's drive, while wip forwards the raw token for wslc to resolve against wip's own directory and drive. Same fix as 01-basic-service — resolve bind sources against compose.yml before forwarding.

### `network` — [09-multi-service-network](fixtures/09-multi-service-network/)

- **wip**: `{"project":"09-multi-service-network","network":"09-multi-service-network"}`
- **upstream**: `{"project":"compose","network":"compose_default"}`
- **status**: `known-difference`
- **note**: With no compose.project, wip names the network after the wip.yml directory and upstream after the compose file's stem — so the same project gets a different network name on each. Same underlying issue as 01-basic-service, shown here without a project: override to make the defaults visible.

### `working_dir` — [10-working-dir-relative](fixtures/10-working-dir-relative/)

- **wip**: `{"app":"app"}`
- **upstream**: `{"error":"the working directory for service \u0027app\u0027 must be an absolute Linux path"}`
- **status**: `known-difference`
- **note**: Upstream refuses a working_dir: that is not an absolute Linux path — a deliberate validation, not a gap. wip passes it through to `wslc run -w`. Upstream's check is the better behaviour and is safe for wip to adopt.

## Upstream unsupported — Compose behaviour `wslc compose` rejects today

### `ports` — [08-ports-extended](fixtures/08-ports-extended/)

- **wip**: `{"app":[{"host_ip":"127.0.0.1","host_port":"8080","container_port":"80","protocol":"tcp"},{"host_ip":null,"host_port":"9090","container_port":"9090","protocol":"udp"},{"host_ip":null,"host_port":"3000-3001","container_port":"3000-3001","protocol":"tcp"},{"host_ip":null,"host_port":"ephemeral","container_port":"8000","protocol":"tcp"}]}`
- **upstream**: `{"error":"the \u0027127.0.0.1:8080:80\u0027 port for service \u0027app\u0027 must use host:container syntax"}`
- **status**: `upstream-unsupported`
- **note**: `wslc compose` rejects the whole file on the first of these: its parser takes exactly one colon, digits only, and no protocol suffix. wip forwards to `wslc run -p`, whose own parser accepts all four. wip is ahead of upstream and matches the Specification — do not narrow it. The reverse gap is 0:8080, which upstream's compose parser accepts as ephemeral and `wslc run -p` rejects.

## wip-only — wip implements it, upstream has no notion of it

### `build` — [11-wip-only-build](fixtures/11-wip-only-build/)

- **wip**: `{"app":{"context":"\u003CFIXTURE\u003E","dockerfile":"Dockerfile.dev","args":{"RUBY_VERSION":"4.0"},"tag":"wip-compose-app:latest"}}`
- **upstream**: `{"error":"the \u0027build\u0027 property is not supported"}`
- **status**: `wip-only`
- **note**: Upstream rejects build: as an unsupported property; it can only run pre-built images. wip stages the context and builds a wip-compose-<service>:latest tag. Migration cannot start for a project with build: until upstream implements it.

### `depends_on` — [12-wip-only-depends-on](fixtures/12-wip-only-depends-on/)

- **wip**: `["db","web"]`
- **upstream**: `{"error":"the \u0027depends_on\u0027 property is not supported"}`
- **status**: `wip-only`
- **note**: Upstream rejects depends_on: and starts services in file order. wip topologically sorts them, so db comes up before web here. Tracked through the resolved start order rather than the raw key, because the order is the observable part.

### `healthcheck` — [13-wip-only-healthcheck](fixtures/13-wip-only-healthcheck/)

- **wip**: `{"db":{"test":["pg_isready","-U","postgres"],"interval":2,"timeout":3,"retries":5,"start_period":1}}`
- **upstream**: `{"error":"the \u0027healthcheck\u0027 property is not supported"}`
- **status**: `wip-only`
- **note**: Upstream rejects healthcheck:. wip normalizes it and `wip up` blocks on it before starting dependents.

### `restart` — [14-wip-only-restart](fixtures/14-wip-only-restart/)

- **wip**: `{"app":"unless-stopped"}`
- **upstream**: `{"error":"the \u0027restart\u0027 property is not supported"}`
- **status**: `wip-only`
- **note**: Upstream rejects restart:. wip reads the policy and supervises the container against it.

### `profiles` — [15-wip-only-profiles](fixtures/15-wip-only-profiles/)

- **wip**: `["seeder"]`
- **upstream**: `{"error":"the \u0027profiles\u0027 property is not supported"}`
- **status**: `wip-only`
- **note**: Upstream rejects profiles:. wip excludes a profile-gated service from the ones it starts — seeder here — since it has no --profile flag to activate one.

### `interpolation` — [16-wip-only-interpolation](fixtures/16-wip-only-interpolation/)

- **wip**: `{"project":"interpolation","network":"interpolation","port_binding_default":"wslc-session-default","start_order":["app"],"services_excluded_by_profiles":[],"services":{"app":{"container_name":"app","image":"ruby:4.0","command_argv":[],"environment":["WIP_COMPAT_RUBY_VERSION=4.0","WIP_COMPAT_WORKDIR=/app","RAILS_ENV=development"],"working_dir":"/app","mounts":[],"ports":[],"network_aliases":["app"],"build":null,"healthcheck":null,"restart":"no"}}}`
- **upstream**: `{"error":"the working directory for service \u0027app\u0027 must be an absolute Linux path"}`
- **status**: `wip-only`
- **note**: Upstream does no interpolation at all: ${WIP_COMPAT_WORKDIR} reaches its working_dir: validation literally and the file is refused. wip substitutes from .env and the shell environment before parsing, so image, working_dir and environment all resolve. Note that wip also merges .env into the container's own environment, which the Specification does not do — worth revisiting separately.

## Fixture index

| fixture | what it is for | features tracked |
|---|---|---|
| [01-basic-service](fixtures/01-basic-service/) | The whole upstream-supported surface in one service: image, exec-form command, environment, working_dir, one bind mount, one published port. | `image`, `command`, `environment`, `working_dir`, `ports`, `container_name`, `service_name`, `service_alias`, `volumes`, `network`, `port_binding_default` |
| [02-command-shell-form](fixtures/02-command-shell-form/) | Shell-form (scalar) command with a quoted argument — the one place the two argv models visibly disagree. | `command` |
| [03-environment-list-form](fixtures/03-environment-list-form/) | environment: written as a KEY=VALUE list rather than a mapping. | `environment` |
| [04-environment-passthrough](fixtures/04-environment-passthrough/) | A mapping entry with no value — the Compose Specification's 'take this variable from the host'. | `environment` |
| [05-container-name](fixtures/05-container-name/) | container_name:, which the Compose Specification defines and wip does not accept. | `container_name`, `service_alias` |
| [06-upstream-name-key](fixtures/06-upstream-name-key/) | A service-level `name:` key — an upstream extension with no Compose Specification counterpart. | `container_name` |
| [07-volume-forms](fixtures/07-volume-forms/) | The three short-syntax volume shapes: a named volume, a relative bind with :ro, and a driveless absolute bind. | `volumes` |
| [08-ports-extended](fixtures/08-ports-extended/) | Port forms the Compose Specification defines beyond plain host:container — a bind address, /udp, a range, and a container-only port. | `ports` |
| [09-multi-service-network](fixtures/09-multi-service-network/) | Two services with no explicit project name — service-to-service naming and the default network. | `service_name`, `service_alias`, `image`, `ports`, `network` |
| [10-working-dir-relative](fixtures/10-working-dir-relative/) | A relative working_dir:, which upstream validates and wip does not. | `working_dir` |
| [11-wip-only-build](fixtures/11-wip-only-build/) | build: with context, dockerfile and args. | `build` |
| [12-wip-only-depends-on](fixtures/12-wip-only-depends-on/) | depends_on: — start ordering between services. | `depends_on` |
| [13-wip-only-healthcheck](fixtures/13-wip-only-healthcheck/) | healthcheck: with the full set of timing fields. | `healthcheck` |
| [14-wip-only-restart](fixtures/14-wip-only-restart/) | restart: policy. | `restart` |
| [15-wip-only-profiles](fixtures/15-wip-only-profiles/) | profiles: gating a service out of the default set. | `profiles` |
| [16-wip-only-interpolation](fixtures/16-wip-only-interpolation/) | ${VAR} interpolation from a project .env file. | `interpolation` |
